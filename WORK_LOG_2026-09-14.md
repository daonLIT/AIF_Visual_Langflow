# 2026-09-14 작업 정리

기준 계획서: `claude_code_langflow_v2_implementation_plan.md` (363줄 전체본)
실행 환경: Langflow Desktop 1.11.0 + Ollama `gemma4:e4b-it-qat`, 백엔드 live 모드
검증 결과 요약: 백엔드 테스트 95개 통과, 프런트엔드 `npm run check`(빌드·lint·smoke 4종) 통과, 실제 Langflow 실행 성공

---

## 1. 초기 작업 (계획서 앞부분 기준)

- 백엔드·프런트 서버 실행·중지.
- 계획서의 앞부분(95줄)을 기준으로 구현했습니다.
  - 쟁점 카탈로그 52개
  - v10 flow
  - 사이트 안 파이프라인 편집기
  - 노드 상세 패널
  - scheme 편집
- `.env`를 채운 뒤 실제 Langflow로 확인했습니다.
  - v10 분석이 약 10분 걸려 성공했습니다.
  - 작업용 복제 → 적용 → 스냅샷 → 복원도 동작을 확인했습니다.
- 작업용 flow에서 Main Claim 프롬프트를 고치고 테스트 실행해, 결과가 주문 문장이 아닌 본안 결론으로 바뀌는 것을 확인했습니다.
- 실행 제한 시간을 2000초로 늘렸습니다(`.env`, `.env.example`, `config.py` 기본값).

## 2. 계획서 전체본 기준 재작업

사용자가 계획서 전체본을 저장한 뒤, 그 내용대로 백엔드·프런트엔드·flow를 다시 만들었습니다.

### 2.1 세부 쟁점 자동 선택 (최대 3개)

- 사용자가 쟁점을 미리 고르는 단계를 없앴습니다(범위 선택 대화상자 삭제). 모델이 판결문을 읽고 52개 중 **중복 없이 최대 3개**를 고릅니다.
- 선택마다 다음을 함께 반환합니다: `issue_id`, 사건별 쟁점 문장, `selection_reason`, `evidence_quote`.
- 선택 개수별 처리
  - 0개: 그래프를 만들지 않고 사유만 보고합니다(`no_issues`).
  - 4개 이상, 카탈로그 밖 ID, 중복: 제한된 횟수만 재시도한 뒤 명시적 오류(`INVALID_SELECTION`)로 처리합니다.
- 서버와 화면이 같은 제약을 다시 검사합니다.
  - ISSUE 편집 선택지 제한
  - 4번째 쟁점 수락 차단
  - 검증 규칙 RULE 14
- 검토 패널에 "세부 쟁점 자동 선택" 보고를 추가했습니다: 선택 이유, 근거 없음/미검출, 가지 추출 실패, 실행 버전.

### 2.2 v11 flow (`langflow/TopDown_Judgment_to_AIF_v11_Top3Issues.json`)

처리 순서는 다음과 같습니다.

```text
Judgment Splitter → Main Claim → Issue Selector(최대 3개) → Issue Branch Extractor
→ AIF Graph Builder → Node Summarizer → Scheme Assigner → Result Validator → Final AIF JSON
```

- 새 커스텀 컴포넌트: `issue_selector.py`, `node_summarizer.py`, `scheme_assigner.py`, `result_validator.py`, 수정한 `aif_graph_builder.py`
- 생성 스크립트 `make_v11_flow.py`는 Langflow 설치본의 lfx로 컴포넌트 template를 만듭니다.

### 2.3 I 노드 요약

- 추가 필드: `summary`, `summaryOrigin(ai|human)`, `summaryStatus(current|stale)`, `summarySourceHash`(본문 FNV-1a 해시)
  - 이 해시는 서버, flow, 프런트가 같은 값을 냅니다.
- 동작
  - 그래프에는 요약을 표시합니다.
  - 클릭하거나 Enter를 누르면 상세 패널에 본문 전체가 열립니다.
  - 편집은 [수정] 버튼 → 저장/취소로만 합니다.
- 본문을 고치면
  - 요약은 "본문 변경됨(stale)"으로 표시됩니다.
  - 그 노드의 근거와 연결된 RA scheme이 재검토 대상이 됩니다.
- [AI 요약 생성](live)
  - 요청한 뒤 본문이나 요약이 바뀐 노드에는 늦게 도착한 결과를 반영하지 않습니다.

### 2.4 RA scheme (`schemeApplication`)

- 필드
  - 기본: `schemeKey`(카탈로그 key, `unclassified`, `custom` 중 하나), `catalogVersion`, `status(suggested|confirmed|needs_review)`, `origin`, `rationale`
  - 전제·결론: `premiseBindings[{roleId,nodeIds}]`, `conclusionNodeIds`
  - 기타: `criticalQuestionResponses`, `notes`, `customSchemeName`, `alternatives`, `history`
- RA 배지는 "RA · 짧은 이름"으로 표시합니다.
- 상세 패널에서 볼 수 있는 항목
  - scheme 선택(미분류·직접 작성 포함), 정의, 출처
  - 역할별 전제, 결론, 비판적 질문, 메모
  - 대안 후보, AI 원안과 수정 이력
- RA의 연결이나 연결된 노드 본문이 바뀌면 scheme을 지우지 않고 "재검토 필요"로 표시합니다.
- 서버 검증
  - 허용되지 않은 key, 역할, 노드 참조는 오류로 기록하고 미분류로 둡니다.
  - namespace를 변환할 때 전제·결론 참조와 `schemefulfillments` 항목의 nodeID도 함께 바꿉니다.
  - `schemefulfillments`는 검증된 외부 schemeID가 있을 때만 만들고, 기존 외부 항목은 보존합니다.

### 2.5 파이프라인 편집

- 서버 API: `/api/pipelines…`, `/test`, `/restore`, `/api/connections/status`
- 초안
  - 편집하면 서버에 자동 저장되고, 새로고침해도 남습니다.
  - 적용본과의 차이를 표시합니다.
- 적용 순서: 검증 → 충돌 검사(`updated_at` + 실행 해시) → 백업 → 저장 → **Langflow에서 다시 읽어 해시 확인** → (선택) 바로 테스트 실행
  - 재조회 결과가 다르면 성공으로 표시하지 않습니다(`APPLY_NOT_VERIFIED`).
  - 적용 메모와 테스트 실행 여부는 적용 대화상자(`ApplyDialog`)에서 정합니다.
- 실행 버전 고정
  - 실행마다 flow 이름·해시, 모델 설정, 입출력 컴포넌트, 카탈로그 버전을 기록합니다.
  - live에서는 해시별 실행용 스냅샷 flow로 실행합니다.
- 연결 확인 항목: langflow, analysis_flow, flow_contract(v11 단계 존재), ollama, ollama_models, catalogs

### 2.6 데이터·저장

- 프로젝트 파일 v2
  - v1 파일과 v10 시절 `scheme` 필드는 불러올 때 자동 변환합니다.
  - 저장할 때 카탈로그 버전을 기록합니다.
- SQLite
  - `PRAGMA user_version` 기반 마이그레이션을 명시적으로 실행합니다.
  - 올리기 전에 백업 파일(`annotation.sqlite3.backup-v{이전}-{시각}`)을 만듭니다.
- AIF import/export
  - 확장 필드를 왕복 보존합니다.
  - 미검토·거절 노드는 내보내지 않습니다.

## 3. 실제 Langflow 연동 검증

| 확인 | 결과 |
| --- | --- |
| v11 flow 가져오기 | API로 새 flow `dfdc515a-…`를 만들었습니다(컴포넌트 ID 유지). `.env`의 `LANGFLOW_FLOW_ID`와 입출력 ID를 이 flow로 바꿨습니다. |
| 첫 실행 2건 | **실패** — "Issue catalog is empty". Langflow가 입력 JSON 안의 `\n` 이스케이프를 실제 줄바꿈으로 바꿔 넘겨, 엄격한 파싱이 실패했습니다. 모든 컴포넌트와 서버 파싱을 `strict=False`로 고치고 회귀 테스트를 추가한 뒤 flow를 다시 반영했습니다. |
| 샘플 판결문 | 약 8분, 성공. 쟁점 3개, 노드 25개, 요약 16/16, flow 검증 통과 |
| 합성 판결문 2 | 약 7분, 성공. 결과가 달랐습니다(쟁점 2개 — 3개 미만 경로 동작). 근거 인용 15개 모두 원문과 일치 |
| 파이프라인 통합 | 작업용 복제 → 프롬프트 수정 → 초안 → 오래된 기준으로 적용 시 409 `CONFLICT` → 정상 적용(재조회 해시 일치) → 테스트 실행 성공. 주 주장이 주문의 "…무죄."에서 이유 결론 "…무죄를 선고한다."로 바뀜 |
| 브라우저 | 세부 쟁점 선택 보고, RA/I 상세 패널, 재검토 표시, live AI 요약 재생성, 서버 저장, 새로고침 후 초안 복원 확인 |

## 4. Walton scheme 10개로 교체 (scheme 카탈로그 v3)

지정한 10개만 남겼습니다: Witness Testimony, Evidence to a Hypothesis, Sign, Inconsistent Commitment, Alternatives, Effect to Cause, Best Explanation, Ignorance, Verbal Classification, an Established Rule.

- **공개 자료와 대조한 7개:** 형식과 비판적 질문을 공개 자료(Walton·Reed·Macagno 2008 기반 CQ 템플릿, Carneades `walton.yml`, 논증 분류 데이터셋 정의 파일)에서 가져왔습니다.
  - 대상: Witness Testimony, Evidence to a Hypothesis, Sign, Alternatives, Ignorance, Verbal Classification, Established Rule
- **원서 대조가 필요한 3개:** 공개 자료에서 원문을 찾지 못해 원서 기준으로 정리했습니다. `verification: needs-book-check`로 표시했습니다.
  - 대상: Inconsistent Commitment, Effect to Cause, Best Explanation
- scheme마다 `sourceNote`(출처)가 있고, RA 상세 패널의 [출처]에서 볼 수 있습니다.
- 새 카탈로그로 live 실행해 성공했습니다(약 8분).
  - RA 9개 중 무지 3개, 결과에서 원인으로 1개, 미분류 5개
  - 억지 분류는 없었고, scheme 결과 검증 오류는 0개였습니다.

## 5. 산출물

| 위치 | 내용 |
| --- | --- |
| `test_outputs/` | live 테스트 결과 4건(`aif_graph.json`, `langflow_output.json`, `langflow_envelope.json`, `annotations.json`, `run_record.json`, `judgment.txt`) + 실패 기록 2건 + `README.md`, `index.json` |
| `backend/fixtures/langflow_run_response.live_v11.json` | 실제 응답 회귀 테스트용 fixture (카탈로그 v3) |
| `backend/catalog/walton_schemes.json` | Walton scheme 10개 (v3) |
| `docs/verification.md` | 검증 기록 전체 |
| `README.md`, `langflow/README.md`, `.env.example` | v11 기준 설명 |

## 6. 환경 변경 사항

- `.env`
  - `LANGFLOW_FLOW_ID`: v11 flow로 변경
  - `LANGFLOW_INPUT_COMPONENT_ID`: `CustomComponent-k5fj9`
  - `LANGFLOW_OUTPUT_COMPONENT_ID`: `ChatOutput-nL1VD`
  - `LANGFLOW_TIMEOUT_SECONDS`: 2000
- Langflow에 추가된 flow
  - v11 flow
  - "AIF v11 작업용 (통합 검증)"
  - 실행용 스냅샷 flow(목록에서는 숨김)
  - 기존 v10 flow와 "AIF v10 작업용 (연동 검증)"은 그대로 둠
- 서버 프로젝트 "live v11 합성 판결문 2" 저장
- `.gitignore`에 `backend/data/captures/` 추가(실제 응답 캡처, 판결문 원문 포함)

## 7. 품질 관찰 (연결 성공과 별개)

- 같은 판결문도 실행마다 선택 쟁점 수가 달라질 수 있습니다(2개 → 3개).
- scheme 적용 이유에 모델 입력용 별칭(`N3` 등)이 섞이는 경우가 있습니다.
- 역할 배정이 느슨한 경우가 있습니다(한 전제를 두 역할에 묶음).
- scheme 목록이 10개로 줄면서 미분류 비율이 늘었습니다.
- 샘플 판결문 실행에서 ISS-028 선택 근거 인용이 원문과 맞지 않아 "근거 없음/미검출"로 표시됐습니다.

## 8. 남은 일

- [ ] Inconsistent Commitment · Effect to Cause · Best Explanation 형식·비판적 질문을 원서와 대조
- [ ] 긴 실제 판결문에서 소요 시간(2000초 제한)과 컨텍스트 한계 확인
- [ ] 브라우저에서 [Langflow 에 적용…] 대화상자를 끝까지 누르는 조작 확인 (같은 흐름은 API로 확인함)
- [ ] 브라우저에서 AIF/OVA 파일 내보내기 → 다시 가져오기 조작 확인 (로직은 smoke 테스트로 확인함)
- [ ] Scheme Assigner 프롬프트 개선 (별칭 노출, 역할 배정)
- [ ] 이전 카탈로그(v2)로 저장된 결과의 scheme key 처리 방침 결정
- [ ] 남은 변경 사항 커밋 (요청 시)
