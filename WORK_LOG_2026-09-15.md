# 2026-09-15 작업 정리

실행 환경: Langflow Desktop 1.11.0 + Ollama `gemma4:e4b-it-qat`, 백엔드 live 모드
검증 결과 요약: 백엔드 테스트 98개 통과, 프런트엔드 `npm run check`(빌드·lint·smoke 4종) 통과, 프로덕션 flow 실제 분석 성공, 정답 그래프 20건 평가 완료

---

## 1. 브라우저 확인 (어제 남은 일)

| 확인 | 결과 |
| --- | --- |
| AIF/OVA 내보내기 → 다시 가져오기 | 통과. "live v11 합성 판결문 2" 전체 수락 → 내보내기 → [JSON 불러오기] → 재내보내기가 **바이트 단위로 동일**. 파일 선택 창은 스크립트로 대체 |
| [Langflow 에 적용…] 대화상자 | 통과. 작업용 flow에 확인용 한 줄 추가 → 적용·재조회 해시 일치 → 테스트 실행 성공(약 9분) → [버전 기록]으로 적용 전 백업 복원 → Langflow 직접 조회로 확인 |

## 2. 파이프라인 툴바 버튼 위치 고정

- **문제:** 초안이 자동 저장되면 [초안 불러오기]·[초안 버리기]가 새로 생겨 뒤 버튼이 밀렸습니다. 확인 중에 [Langflow 에 적용…] 대신 [초안 버리기]가 눌렸습니다.
- **수정 (`PipelineEditor.tsx`, `index.css`)**
  - 두 버튼을 항상 표시하고 초안이 없으면 비활성화
  - 상태 표시("적용 안 된 편집 · …")는 남는 폭만 쓰고 넘치면 말줄임
- **확인:** 초안 없음 / 저장 대기 / 저장됨 세 상태에서 버튼 12개 좌표와 툴바 높이(47px)가 모두 같음

## 3. 이전 scheme 카탈로그(v2) key 전환

- **대응표:** `backend/catalog/scheme_catalog_migrations.json`
  - 서버가 시작할 때 v3에 대상 key·역할·CQ가 있는지 검사합니다.
  - `/api/catalogs/schemes`의 `migrations`로 내보냅니다.
- **처리 규칙 (자동 확정 없음, 원래 key·역할·CQ 응답은 수정 이력 "카탈로그 전환"에 보존)**

| 처리 | key |
| --- | --- |
| key 유지 (역할·CQ 번호만 맞춤) | sign, witness_testimony, verbal_classification, evidence_to_hypothesis(CQ2→CQ3), inconsistent_commitment(역할 이름 변경), established_rule(facts→applicability, 항상 재검토) |
| 새 key로 교체 + 재검토 | lack_of_evidence→ignorance, abduction→best_explanation |
| 미분류 + 재검토 + 대안 후보 | position_to_know·credibility_assessment → witness_testimony 후보 |
| 미분류 + 재검토 (후보 없음) | convergent_facts, cause_to_effect, correlation_to_cause, expert_opinion, perception, analogy, bias |

- **적용 경로:** 프로젝트 열기, JSON·AIF 불러오기(`importAifOva`, `migrateProjectFile`). 과거 실행 기록은 바꾸지 않습니다.
- **테스트:** 백엔드 대응표 검사 2개, 프런트 smoke 24개(실제 v2 결과 포함)
- **브라우저 확인:** 서버 프로젝트를 열자 안내가 뜨고 RA가 "무지(증거 부재) · 재검토"로 바뀌며, 이력에 이전 key·역할·CQ 응답이 표시됨

## 4. Scheme Assigner 개선 (프로덕션 반영)

### 4.1 바꾼 것

- **컴포넌트 (`langflow/components/scheme_assigner.py`)**
  - 한 전제를 두 역할에 넣으면 오류로 알려 재시도, 끝내 고치지 않으면 처음 역할만 남김
  - 설명·CQ 답·대안 이유에 섞인 모델 입력용 별칭(R1, N2…)을 그 그룹에 실제로 준 별칭만 제거 (판결문의 "N95" 같은 표기는 유지)
- **프롬프트 v2 (`make_v11_flow.py`)**
  - ISSUE 노드도 다른 노드와 똑같이 판단
  - 채울 전제가 없는 역할이 있어도 그 이유만으로 미분류로 두지 않음
  - 본문에 없는 규칙·일반화를 지어내지 않음
  - 모든 RA에 이유 작성
- **v1은 버림:** "ISSUE는 질문"이라고 적었더니, 판단을 서술한 ISSUE가 많은 샘플 판결문에서 RA 4개가 미분류로 떨어졌습니다.

### 4.2 비교 결과

저장된 그래프 4개(RA 33개)에 같은 입력으로 A/B 비교:

| 지표 | 기존 | v2 |
| --- | --- | --- |
| 별칭 노출 | 31 | 0 |
| 한 노드 여러 역할 RA | 10 | 0 |
| 미분류 | 12 | 10 |
| Scheme 단계 시간 | 897초 | 1308초 |

- **작업용 flow 라이브:** 성공 588초. 별칭 0, 여러 역할 0
- **프로덕션 반영**
  - 편집기 적용 경로(`PipelineService.apply`)를 쓰고, 프로덕션 보호 검사만 1회 우회
  - 반영 전 해시 `42fa72b0…` → 반영 후 `47991b91…`(재조회 일치), 백업 버전 `07042645…`
- **프로덕션 실제 분석 (합성 판결문 2, `c00e6e26`):** 성공 606초. 미분류 5→4, 별칭 6→0, 여러 역할 2→0
- **결정 사항:** 법리 해석에 해당하는 규칙(예: 언어적 분류 논증의 적용 범위)은 넣지 않기로 함. 시간 증가는 괜찮음

## 5. 정답 그래프 평가 체계

### 5.1 자료

| 폴더 | 내용 | git |
| --- | --- | --- |
| `judgment/` | 판결문 JSON 580건 (`{id, judgment}`, 대부분 "피고인 및 변호인의 주장에 대한 판단" 발췌, 중앙값 3,423자, 최장 20,115자) | 제외 |
| `eval/` | 사람이 만든 정답 그래프 20건. 쟁점 문장을 카탈로그 이름으로 맞춤 (64개 중 60개 ID 대응, 4개는 단락 제목) | 제외 |

- 정답 그래프 특징
  - I 노드는 판결문 원문 문장 그대로입니다(255개 중 252개).
  - 쟁점 노드는 **RA 없이** I 노드에 바로 연결되고, RA는 I 노드끼리의 추론에만 있습니다.
- 데이터 문제: `CASE000210201703292016075001243` 정답 파일에 없는 노드를 가리키는 연결 2개

### 5.2 평가 도구 (`backend/scripts/`, 아직 커밋 안 함)

| 파일 | 역할 |
| --- | --- |
| `eval_runs.py` | 평가 세트(정답 20건 + 최장 1건)를 차례로 분석하고 `test_outputs/eval/<라벨>/`에 실행 기록·`summary.md`·`summary.json` 저장. 같은 라벨로 다시 실행하면 이어서 함 |
| `eval_metrics.py` | 구조 지표: 쟁점 문장이 이름뿐인지, 근거 없음, 제목형 I 노드, 미분류, 여러 역할, 역할 채움, 별칭·임의 이름 노출, 검증, 시간 |
| `eval_gold.py` | 정답 비교: 쟁점 적중/선택/정답, 정답 I 노드를 생성 근거가 덮는 비율, 생성 근거 중 정답과 겹치는 비율, 주 주장 일치, RA 수, 쟁점에 붙은 RA 수 (scheme은 비교하지 않음) |

- 정답 그래프를 생성 결과처럼 넣어 점검: 모든 비교 지표가 만점으로 나옴
- 제목형 판정 보정: 판결문의 "…한 점/사실" 문장을 명제로 인정하고 40자 이하만 판정

### 5.3 프로덕션 기준 평가 (`test_outputs/eval/prod-47991b91/`)

| 영역 | 결과 |
| --- | --- |
| 실행 | 20/20 성공, 평균 741초(539~925초), 총 약 4시간 7분 |
| 최장 판결문(20,115자) | **실패** 154초. 쟁점 선택 단계가 3회 모두 빈 응답 → `INVALID_SELECTION`. 컨텍스트(16,384토큰) 초과로 추정, 미확인 |
| 쟁점 선택 | 고른 60개 중 정답과 같은 ID **18개(30%)**, 상위 쟁점군 기준 36개(60%). 정답 53개 중 34% 찾음. 항상 3개 선택(정답 평균 2.65개) |
| 쟁점 문장 | **9/60이 카탈로그 이름뿐** |
| I 노드 근거 | 생성 근거 281개 중 247개(88%)가 정답 문장 안. 정답 I 노드 255개 중 102개(40%)만 덮음 |
| 주 주장 | 정답에 최상위 노드가 있는 16건 중 13건 일치 |
| RA 구조 | 생성 176개(정답 92개), **그중 118개(67%)가 쟁점 노드에 붙은 RA** |
| 미분류 | 37/176. 쟁점에 붙은 RA 30/118(25%), 나머지 7/58(12%) |
| scheme 형식 | 여러 역할 0, 별칭 0, 임의 이름 3 |

- 선택 경향
  - 정답에 자주 나오는 "객관·다른 진술과의 부합"(정답 8회·선택 2회), "사건 전후 행동"(정답 4회·선택 0회)은 거의 고르지 않음
  - 정답에 거의 없는 "체액·혈흔 검출"(선택 7회·정답 1회), "음주·의식 상태"(선택 5회·정답 0회)는 자주 고름

## 6. 커밋 상태

- 커밋됨: 툴바 수정, v2 key 전환, Scheme Assigner 개선 (`685c937 Scheme Assigner 프롬프트 개선`까지)
- **커밋 안 됨:** `backend/scripts/eval_gold.py`, `eval_metrics.py`, `eval_runs.py`, `.gitignore`(`judgment/` 추가, 사용자 변경)

---

## 내일 할 일

### 먼저 할 것

- [ ] **평가 도구 커밋** — `backend/scripts/eval_*.py`와 `.gitignore`
- [ ] **쟁점 노드 연결 구조 변경 여부 확인 후 구현** — AIF Graph Builder가 쟁점 노드를 RA 없이 I 노드에 바로 연결하도록 (정답 구조와 맞춤)
  - 근거: 생성 RA 176개 중 118개가 쟁점 주변 RA이고, 미분류 37개 중 30개가 여기서 나옴
  - 그래프 모양이 바뀌므로 프런트 검증 규칙·검토 패널·기존 프로젝트 호환도 함께 확인
- [ ] **쟁점 문장이 카탈로그 이름뿐이면 재시도** — Issue Selector 검증 규칙 추가 (60개 중 9개 발생)
- [ ] **긴 판결문 실패 원인 확정과 대응**
  - 쟁점 선택 단계의 실제 입력 토큰 수 측정
  - 컨텍스트 창을 늘릴지, 판결문을 나눠 넣을지 결정
  - 16,000자 넘는 판결문 7건이 같은 문제를 겪을 수 있음
- [ ] **위 변경을 작업용 flow에 적용하고 같은 21건으로 재평가** — 기준값 `prod-47991b91`과 비교 (약 4시간, 백그라운드)

### 결정이 필요한 것

- [ ] **쟁점 선택 정확도(ID 기준 30%)를 이 프로젝트에서 다룰지** — 법리 해석 범위인지 판단 필요. 다룬다면 정답 사례를 몇 개 넣는 방식 검토
- [ ] **I 노드를 원문 문장 그대로 뽑게 할지** — 정답과 비교는 쉬워지지만 그래프 문장이 길어짐 (현재 정답 덮음 40%)
- [ ] **Main Claim "이유 결론 우선" 프롬프트를 프로덕션에 반영할지** — 작업용 flow에서만 확인됨. 재평가로 전후 비교 후 결정 권장

### 짧게 끝나는 것

- [ ] 서버 프로젝트 "live v11 합성 판결문 2"를 열어 [서버에 저장] — 파일에 v3 key 반영
- [ ] 정답 파일 `CASE000210201703292016075001243`의 끊긴 연결 2개 정리 (사용자 확인)
- [ ] Scheme Assigner가 설명에 스스로 붙인 이름("C2" 등) 거르기 — 평가 20건에서 3건

### 자료가 필요한 것

- [ ] Inconsistent Commitment · Effect to Cause · Best Explanation 형식·비판적 질문 원서 대조
