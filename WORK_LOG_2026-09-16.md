# 2026-09-16 작업 정리

오늘은 **새 실행 없이 `prod-47991b91` 평가 결과 20건을 재분석**했다. 판결문·정답 그래프·실행 기록을 직접 파고들어 어제 요약에서 안 보이던 것들을 확인했고, 그 과정에서 어제 세운 우선순위를 두 번 뒤집었다.

분석 대상: `test_outputs/eval/prod-47991b91/` (flow `47991b91`, 20/20 성공 + 최장 1건 실패)

---

## 1. 알아낸 것

### 1.1 "정답 I 덮음"이 실제로 재는 것

`eval_gold.py`의 `_covered()`는 **원문 글자 구간 겹침**만 본다. 노드 내용은 전혀 비교하지 않는다.

1. 정답 I 노드는 판결문 원문 문장 그대로 → 판결문 안에서 `[시작, 끝]` 구간을 가짐
2. 생성 I 노드는 근거 인용 구간(`evidence`의 `start`/`end`)을 가짐
3. 생성 근거 구간들의 **합집합이 정답 구간의 50% 이상을 덮으면** 덮음 (`OVERLAP = 0.5`)

따라서:

- 문장을 다시 써도, 둘을 합쳐도, **그 대목으로 엉뚱한 명제를 만들어도 덮음으로 센다**
- 노드 대 노드 매칭이 아니다 — 긴 인용 하나가 정답 노드 여러 개를 한 번에 덮는다
- 구조(어느 쟁점 밑인지, RA가 맞는지)는 안 본다

**즉 덮음률 40%는 "정답과 40% 일치"가 아니라 "정답이 쓴 원문 대목의 40%만 건드렸다"는 너그러운 하한이다.** 실제 내용 일치율의 상한으로 읽어야 한다.

### 1.2 임계값은 사실상 작동하지 않는다 — 이분법이다

정답 I 노드 252개의 겹침 분포:

| 겹침 | 개수 | 판정 |
| --- | --- | --- |
| **0%** (아예 안 건드림) | **136 (54%)** | 미덮음 |
| 0~50% (일부만 인용) | 14 (6%) | 미덮음 — 임계값이 작용한 유일한 구간 |
| 50~120% | 53 (21%) | 덮음 |
| **120% 초과** (중복 인용) | **49 (19%)** | 덮음 |

미덮음 150개 중 **136개(91%)가 겹침 0%**. "비슷한데 아깝게 탈락"이 아니라 **그 문단을 통째로 안 읽었다**. 지표가 야박해서 낮게 나왔다는 해석은 성립하지 않는다.

겹침 120% 초과 19%는 **중복 인용**이다 (`2075001384` 정답 12번은 551% — 같은 혈흔/DNA 대목을 5중 인용).

### 1.3 놓친 것의 정체: 쟁점 소속이 아니라 중간 추론 노드

정답 I 노드를 소속 쟁점별로 나눴다 (ISSUE에서 정방향 도달, 배타적 소유):

| 구분 | 개수 | 덮음 |
| --- | --- | --- |
| 선택한 쟁점 바로 밑 | 22 (9%) | **16 (73%)** |
| 못 고른 쟁점 바로 밑 | 48 (19%) | 19 (40%) |
| **공통·상위 (여러 쟁점이 함께 떠받치는 중간 추론)** | **182 (72%)** | **67 (37%)** |

**정답 그래프의 72%가 특정 쟁점에 속하지 않는 중간 추론 노드이고, 놓친 150개 중 115개가 여기 있다.**

쟁점 선택을 완벽하게 만들어도 상한은 낮다 — 못 고른 48개가 "선택한 쟁점 밑" 수준(73%)으로 덮인다고 가정해도 +16개, 덮음률 **40% → 47%** (+7포인트).

### 1.4 길이가 길수록 나빠진다 — 원인은 고정된 그래프 크기

| 상관 | r |
| --- | --- |
| 길이 ↔ 덮음률 | **−0.60** |
| 길이 ↔ 근거 정밀도 | −0.56 |
| 길이 ↔ 쟁점 재현율 | +0.18 (무관) |

짧은 10건(≤4,075자) 평균 덮음 **59%** vs 긴 10건 **35%**.

원인은 명확하다:

| | 길이와의 상관 | 범위 |
| --- | --- | --- |
| **생성 RA 수** | +0.22 | **7~9** (20건 중 18건이 정확히 9) |
| 생성 I 수 | +0.37 | 10~24 (평균 14.6) |
| **정답 I 수** | **+0.73** | 5~21 |

판결문이 길어지면 정답 노드는 늘어나는데 생성 노드는 안 늘어난다. 덮음률은 산술적으로 떨어질 수밖에 없다.

- 755자 / 정답 I 5개 → 생성 10개로 **100%** 덮음
- 10,531자 / 정답 I 21개 → 생성 14개로 **19%** 덮음

### 1.5 환각은 없다 — 문제는 전적으로 "어디를 고르는가"

생성 I 노드 291개 기준:

| | 개수 |
| --- | --- |
| 근거 있음 | 281 (97%) |
| 근거 구간이 원문 범위를 벗어남 | **0** |
| 인용문이 원문과 글자 단위 일치 | 268/281 (95%, 나머지는 공백 차이) |
| **근거 없음** | **10 (3%)** ← 원문 대조 자체가 불가, 유일한 사각지대 |

정답과 안 겹치는 근거 34개도 **전부 실제 원문 문장**이다. 지어낸 게 아니라 정답이 I 노드로 안 쓴 대목을 골랐다. 위치는 판결문 20~40% 지점에 몰려 있고(13/34), 내용은 **배경 사실관계 서술**이다:

```
[6075000991] 피고인, 피해자와 J은 곱창집에서 같은 날 21:25까지 소주 4병, 맥주 3병을 마셨다.
[6075000991] 피해자는 자신의 주량에 관하여 '소주 반병 정도를 마시면 …'라고 진술한다.
```

어제 확인한 선택 편향("음주·의식 상태" 선택 5회 / 정답 0회)이 노드 수준에서도 같은 모양으로 나타난다.

파이프라인이 스스로 `unmatched`로 표시한 근거 15개도 **원문 이탈이 아니라 인용 중간이 잘린 것**이었다 — 원문과 최장 일치 55~99%(중앙값 74%), 끊긴 지점 직후 원문이 그대로 이어진다.

### 1.6 미측정 영역 세 겹

`eval_metrics.py`는 전부 세기와 형식 검사, `eval_gold.py`는 글자 구간 겹침이다. **의미를 보는 지표는 하나도 없다.**

| 층 | 질문 | 상태 |
| --- | --- | --- |
| ① 근거 ↔ 주장 | 인용한 문장이 그 노드의 주장을 뒷받침하나 | **미측정** |
| ② 노드 ↔ 쟁점 | 그 노드가 매달린 쟁점과 관련 있나 | 부분 측정 (표본 부족) |
| ③ 전제 ↔ 결론(RA) | 전제들이 결론을 지지하나 | **의도적 제외** (법리 해석) |

①의 예: `2075001384` 정답 13번은 원문이 "노끈이 발견되었다"는 사실만 말하는데, 생성 노드는 "이는 성관계를 위한 폭행으로 보는 것이 합리적"이라는 **판단을 덧붙였다**. 현재 지표로는 100% 덮음·100% 정밀로 잡힌다.

②는 정답 그래프로 구조적으로 재봤다 (덮인 정답 I 노드의 소속 쟁점 비교, 표본 49개):

| | 개수 |
| --- | --- |
| 같은 쟁점 밑 | 8 (16%) |
| 다른 쟁점 밑 | 4 (8%) |
| 생성 쪽 쟁점 불명(공통·상위) | 37 (76%) |

표본이 작아 8%라는 수치는 신뢰 구간이 넓다. 확실한 건 76%가 비교조차 안 된다는 점이고, 이는 1.3의 벽과 같다.

### 1.7 3개·9개는 구조 문제다 (하드코딩)

**쟁점 3개** — `langflow/components/issue_selector.py:12`, `issue_branch_extractor.py:13`의 `MAX_SELECTED = 3`. 초과하면 예외 (`issue_branch_extractor.py:190`).

**RA 9개** — `langflow/components/aif_graph_builder.py`가 쟁점 하나당 **항상 정확히 RA 3개**를 찍는다:

```python
ra_claim = next_id()   # L153  ISSUE  → RA → 주 주장
ra_upper = next_id()   # L189  upper I → RA → 주 주장
ra_lower = next_id()   # L190  lower I들 → RA → upper I
```

3 쟁점 × 3 = 9. RA가 7인 2건은 분기 추출이 실패해 `ra_claim`만 남은 경우다. **RA 수는 모델이 정하는 게 아니라 쟁점 수의 함수다.** 가변인 건 맨 아래 `lower_i_nodes` 개수뿐이다.

정답과 비교:

| | 정답 | 생성 |
| --- | --- | --- |
| I 노드 체인 깊이 | 2~4 (평균 2.9) | **3~3, 전 건 동일** |
| RA 수 | 2~8 (평균 4.6) | 7~9 (18건이 정확히 9) |
| RA당 전제 수 | 1~6 (평균 2.5) | 1~9 (평균 1.9) |
| 쟁점 수 | 1~4 (평균 2.6) | **항상 3** |

제약이 세 겹이다:

1. **쟁점 3개 고정** — 정답이 1~2개인 건에는 과잉, 4개인 건에는 부족
2. **깊이 3 고정** — 정답 깊이 4인 건은 표현 자체가 불가능
3. **쟁점당 상위 노드 1개 고정** — 가장 큰 제약

3번이 1.3의 "공통·상위 72% 미덮음"의 정체다. `2075001384` 정답 쟁점 3의 모양:

```
I19, I20 → RA18 → I15 ┐
                      ├→ RA17 → I2 → RA3 → I1
I22, I23 → RA21 → I16 ┘
```

한 쟁점 밑에 **병렬 하위 갈래 2개**가 있고 그 위에 공통 결론이 있다. 현재 빌더는 쟁점당 `upper` 하나만 허용하므로 **이 모양을 만들 수 없다** — 값을 잘 뽑아도 담을 그릇이 없다.

### 1.8 잘 나온 케이스

상위 3건 (전 지표가 좋은 건은 없음):

| 사건 | 길이 | 쟁점 적중/선택/정답 | 덮음 | 근거 정밀도 | 주주장 | 미분류 |
| --- | --- | --- | --- | --- | --- | --- |
| `2075001384` | 2,405 | 2/3/3 | 6/13 (46%) | 12/12 (100%) | O | 0/9 |
| `8075000380` | 4,346 | 2/3/2 (정답 쟁점 전부 적중) | 6/13 (46%) | 19/19 (100%) | — | 2/9 |
| `7075001093` | 938 | 1/3/3 | 5/5 (100%) | 14/14 (100%) | O | 1/9 |

하위 3건: `9075000485`(쟁점 0적중, 덮음 4/16), `9075000106`(쟁점 0/3/1), `6075001228`(덮음 3/18, 정밀 7/14).

### 1.9 참고: 현재 모델 설정

`gemma4:e4b-it-qat` @ `http://localhost:11434`, temperature 0.1 (4개 LLM 단계 전부 동일). 모델명은 `.env`가 아니라 Langflow flow 노드에 박혀 있고, `make_v11_flow.py:413-416`이 Main Claim LLM 노드의 설정을 나머지에 복사한다.

| 단계 | num_ctx |
| --- | --- |
| selector / extractor / assigner | 16,384 |
| summarizer | 8,192 |

최장 판결문(20,115자) 실패가 selector의 16,384 한계와 맞닿아 있을 가능성 (미확인).

---

## 2. 판단 수정 이력

기록해 둔다. 같은 실수를 반복하지 않기 위해서다.

| 시점 | 판단 | 근거 | 결과 |
| --- | --- | --- | --- |
| 어제 | 쟁점 선택 정확도(30%)가 최우선 | 표면 수치 | — |
| 오늘 1차 | 쟁점 선택이 덮음률의 최대 레버 | `2075001384` 한 건 일반화 | **틀림** |
| 오늘 2차 | 병목은 중간 추론 노드(72%) | 쟁점 소속별 분해 | 유지 |
| 오늘 3차 | 근본 원인은 고정 토폴로지 | 코드 + 깊이/RA 분포 | **현재 결론** |

교훈: 잘 나온 한 건의 패턴을 전체로 일반화하지 말 것. 케이스 단위로 분해해서 확인할 것.

---

## 3. 할 일

### 3.1 어제 목록에서 아직 안 한 것

- [ ] **평가 도구 커밋** — `backend/scripts/eval_*.py`와 `.gitignore` (여전히 미커밋)
- [ ] **긴 판결문 실패 원인 확정** — selector 단계 실제 입력 토큰 수 측정, 컨텍스트 확대 vs 분할 결정. 16,000자 넘는 판결문 7건이 같은 위험
- [ ] 서버 프로젝트 "live v11 합성 판결문 2" 열어 [서버에 저장] (v3 key 반영)
- [ ] 정답 파일 `CASE000210201703292016075001243`의 끊긴 연결 2개 정리
- [ ] Inconsistent Commitment · Effect to Cause · Best Explanation 원서 대조

### 3.2 오늘 분석으로 범위가 명확해진 것

- [ ] **`ra_claim` 제거** (`aif_graph_builder.py:153`) — 쟁점 노드를 RA 없이 I 노드에 직접 연결
  - 근거: 미분류 37개 중 30개가 쟁점에 붙은 RA에서 발생
  - 효과 예상: 미분류 크게 감소, RA 9→6. **덮음률은 그대로** (깊이·갈래를 안 건드림)
  - 프런트 검증 규칙·검토 패널·기존 프로젝트 호환 함께 확인
- [ ] **쟁점 문장이 카탈로그 이름뿐이면 재시도** — Issue Selector 검증 규칙 (60개 중 9개). 순수 구조 규칙이라 값싸다
- [ ] **근거 없는 I 노드 10개 / 인용 잘린 근거 15개 검증 규칙 추가** — 둘 다 값싸게 잡힌다
- [ ] **위 변경 후 같은 21건 재평가** — 기준값 `prod-47991b91`과 비교 (약 4시간, 백그라운드)
  - **한 번에 하나씩 바꿀 것.** 둘 이상 동시에 바꾸면 무엇이 무엇을 움직였는지 분리가 안 된다

### 3.3 설계가 필요한 것 (별도 건)

- [ ] **그래프 토폴로지 가변화** — 덮음률 40%의 근본 대응
  - 쟁점당 상위 노드를 여러 개 허용 (병렬 갈래)
  - 깊이를 3 고정에서 가변으로
  - 쟁점 수를 판결문에 맞게 (현재 항상 3, 정답은 1~4)
  - 영향 범위: `lower_i_nodes` 평면 리스트 계약 → 재귀 구조. 분기 추출 프롬프트·스키마·`aif_graph_builder`·프런트 검증이 함께 움직인다. **적지 않은 작업**
- [ ] **① 근거 ↔ 주장 지표 추가** (`eval_gold.py`) — "인용문에 없는 판단이 주장에 추가됐는가"를 구조적으로 근사. 현재 완전 미측정
- [ ] ③ RA 타당성은 **계속 제외** — 법리 해석 영역

### 3.4 결정이 필요한 것

- [ ] **쟁점 선택 정확도(30%)를 이 프로젝트에서 다룰지**
  - 덮음률 관점에서는 후순위 (상한 +7포인트)
  - 다만 쟁점 목록 자체가 사용자에게 보이는 산출물이라 그 자체로 문제
  - 정답 사례 주입 방식은 법리 해석 범위 판단이 선행되어야 함
- [ ] **I 노드를 원문 문장 그대로 뽑게 할지** — 정답 비교는 쉬워지나 그래프 문장이 길어짐
- [ ] **Main Claim "이유 결론 우선" 프롬프트를 프로덕션에 반영할지** — 작업용 flow에서만 확인됨

### 3.5 보류

- [ ] **oracle 실험(정답 쟁점 강제 주입 후 5건 재실행, 약 1시간)** — 1.3에서 상한을 산술로 추정(+7포인트)했으므로 급하지 않다. 3.2의 변경을 먼저 하고, 재평가 결과가 예상과 크게 다르면 그때 검증용으로 쓴다
