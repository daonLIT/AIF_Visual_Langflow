# 2026-09-17 작업 정리 (09-16 저녁 ~ 09-17 새벽)

실행 환경: Langflow Desktop 1.11.0 + 서버(spark-a164) Ollama 0.30.10 `gemma4:26b` (SSH 터널), 백엔드 live 모드
검증 결과 요약: 모델을 `gemma4:26b`로 바꾼 flow를 새로 올려 실제 분석 성공, 5,000자 이하 판결문 100건 평가 **100/100 성공**, 백엔드 테스트 102개 통과

---

## 1. 연결 점검 — 처음엔 왜 안 돌았나

`.env`에 flow ID를 넣고 SSH 터널을 띄운 상태였지만 실행이 불가능했다. 막힌 곳은 세 군데였다.

| # | 문제 | 원인 | 조치 |
| --- | --- | --- | --- |
| 1 | Ollama 연결 불가 | 터널이 `-L 11500:localhost:11434`로 떠 있었음. flow의 모든 LLM 노드는 `http://localhost:11434`를 호출 | 터널을 `ssh -N -L 11434:localhost:11434 litailab01@210.115.229.70`로 다시 띄움 (사용자) |
| 2 | `.env`가 잘못된 대상을 가리킴 | UI로 가져온 flow `5e7aeb76`을 가리켰고, 입력 컴포넌트 ID에 오타(`CustomComponent-Cykd`, 끝의 `z` 누락)가 있었음 | 새로 올린 flow 기준으로 세 값 교체 (아래 2.2) |
| 3 | 모델이 gemma가 아님 | 마지막 커밋의 `make_v11_flow.py`가 `qwen36-27b-q8-ctx32k`로 설정. UI로 가져온 flow는 Main Claim LLM의 `model_name`이 **빈 값** | 생성기 모델 교체 후 flow를 다시 만들어 API로 가져옴 (아래 2.1) |

참고:

- 예전 프로덕션 flow `dfdc515a`는 `gemma4:e4b-it-qat`로 되어 있는데, **서버에는 이 모델이 없다** (`gemma4:e4b`, `gemma4-e4b-ctx32k`만 있음). 이 flow는 현재 서버로는 돌지 않는다.
- 서버 모델 목록 (2026-09-16 기준): `gemma4:26b`(25.8B Q4_K_M, 원래 컨텍스트 262,144, thinking 지원), `qwen36-27b-q8-ctx32k`, `qwen36-27b-q8-ctx32k-np`, `qwen3.6:27b-q8_0`, `gemma4-e4b-ctx32k`, `gemma4:e4b`, `qwen3.5:35b-a3b`, `translategemma:12b`, `bge-m3` 등
- `gemma4:26b` 단독 호출: 모델 로드 15초, 짧은 응답까지 26초

---

## 2. 변경 사항

### 2.1 flow 생성기 모델 교체 — `langflow/make_v11_flow.py`

- `MODEL_NAME = "gemma4:26b"` (이전 값 `qwen36-27b-q8-ctx32k`는 주석과 드롭다운 후보에 남김)
- `MODEL_OPTIONS`에 `qwen36-27b-q8-ctx32k` 추가
- 커스텀 컴포넌트 코드는 건드리지 않음 → 빌드 캐시(`_built_nodes.json`) 지문이 그대로라 `--offline`으로 재생성 가능
- `langflow/TopDown_Judgment_to_AIF_v11_Top3Issues.json` 재생성: 11 노드 / 20 엣지, LLM 노드 5개 모두 `gemma4:26b`

| 단계 | num_ctx | think |
| --- | --- | --- |
| Main Claim LLM | 32,768 | (옵션 없음) |
| Issue Selector | 32,768 | 끔 |
| Issue Branch Extractor | 32,768 | 끔 |
| I-node Summarizer | 8,192 | 끔 |
| RA Scheme Assigner | 32,768 | 끔 |

### 2.2 Langflow에 새 flow 등록 + `.env`

API(`POST /api/v1/flows/`)로 가져와 **노드 ID가 유지**되게 했다 (UI로 가져오면 ID가 새로 붙는다).

| 항목 | 값 |
| --- | --- |
| 이름 | `TopDown_Judgment_to_AIF_v11_Top3Issues (gemma4:26b)` |
| Flow ID | `e8ee8cec-1ddb-519c-a2e0-a61e17930990` (생성기의 uuid5 시드 값) |
| `LANGFLOW_INPUT_COMPONENT_ID` | `CustomComponent-k5fj9` |
| `LANGFLOW_OUTPUT_COMPONENT_ID` | `ChatOutput-nL1VD` |

- 기존 flow `dfdc515a`(프로덕션)와 `5e7aeb76`(UI로 가져온 사본)은 건드리지 않음. `5e7aeb76`은 이제 쓰지 않으니 지워도 된다.
- 백엔드 `/api/pipelines` 확인: `mode=live`, `productionFlowId`와 `analysisFlowId` 모두 `e8ee8cec…`

### 2.3 평가 실행기 확장 — `backend/scripts/eval_runs.py`

밤새 무인으로 돌리기 위해 추가했다.

| 옵션 / 동작 | 내용 |
| --- | --- |
| `--set all` | gold21을 먼저 돌리고, `judgment/`의 나머지를 **고정 시드(0)로 섞은 순서**로 이어서 실행. 중간에 끊겨도 앞부분이 길이에 치우치지 않게 하려는 것 |
| `--max-length N` | N자보다 긴 판결문을 뺀 뒤 `--limit` 적용 |
| 연결 대기 | 매 건 시작 전 Ollama(`--ollama-url`, 기본 `http://localhost:11434`)와 중계 서버 응답 확인. 끊겨 있으면 실패로 넘기지 않고 60초 간격으로 복구를 기다림 |
| 재시도 | 실행이 실패했는데 **그 직후 Ollama가 끊겨 있으면** 복구 뒤 재시도 (`--retries`, 기본 2). Ollama가 살아 있던 실패는 재시도하지 않음 |
| 중계 서버 폴링 | 결과 조회 중 서버 연결이 끊기면 복구를 기다렸다가 이어서 조회 |

기존 동작(이미 성공한 건은 건너뛰기)은 그대로다.

### 2.4 검증

- 백엔드 테스트: `AIF_SKIP_DOTENV=1 python run_tests.py` → **102개 통과**
- 실제 실행 1건 (755자, `CASE000210201607152016075000596`): 215초, 성공, 검증 통과

---

## 3. 100건 평가 — `gemma4-26b-short100`

### 3.1 실행 개요

580건 전체를 먼저 시작했으나, 건당 4~5분이면 약 48시간이 걸려 **5,000자 이하 100건**으로 줄였다.

```
python scripts/eval_runs.py --label gemma4-26b-short100 --set all --max-length 5000 --limit 100
```

| 항목 | 값 |
| --- | --- |
| 표본 | 5,000자 이하 판결문 중 정답이 있는 13건 + 섞은 순서로 고른 87건 |
| 길이 | 최소 426 / 중앙값 2,427 / 최대 4,956자 (전체 580건 중앙값은 3,423자) |
| 기간 | 2026-09-16 20:20경 ~ 09-17 02:45경 |
| 결과 | **100/100 성공**, 그래프 없음 0, 검증 오류 0, 터널 끊김 0 |
| 시간 | 총 22,616초 (6시간 17분), 건당 최소 133 / 중앙값 215 / 평균 226 / 최대 401초 |
| 길이 ↔ 시간 상관 | r = 0.43 |

정답이 있는 21건 중 8건은 5,000자가 넘어 빠졌다 (7,186~10,531자 5건 포함, 최장 20,115자 건 포함).

전환 과정: 580건 실행의 1번 건이 끝난 뒤 스크립트를 멈췄다. 2번 건은 이미 백엔드에 들어가 있어 끝나기를 기다려 결과를 저장했고, 결과 폴더 이름을 바꾼 뒤 100건 실행으로 재개했다. 두 건 모두 100건 세트에 포함되므로 중복 실행은 없었다.

### 3.2 정답 13건 비교 — 기준값 `prod-47991b91`(gemma4:e4b-it-qat)과 같은 사건

| 지표 | 기준값 | gemma4:26b | 변화 |
| --- | --- | --- | --- |
| 걸린 시간 합계 | 9,082초 | **3,014초** | 약 3배 빠름 |
| 정답 I 노드를 덮은 수 | 66/143 (46%) | **81/143 (57%)** | +15 |
| 쟁점 적중 / 선택 (정답 36) | 12 / 39 | 14 / 49 | 적중 +2, 선택 +10 |
| 쟁점 정밀도 | 31% | 29% | 그대로 |
| 생성 근거 중 정답과 겹침 | 172/177 | 158/168 | 비슷 |
| 주 주장 일치 | 10 | 9 | −1 |
| I 노드 | 179 | 195 | +16 |
| RA | 113 | 111 | 비슷 |
| **미분류 RA** | 27 | **3** | −24 |
| 쟁점에 붙은 RA | 76 | 62 | −14 |
| **근거가 원문과 안 맞음 (`unmatched`)** | 6 | **38** | +32 |
| 원문에서 근거를 못 찾은 쟁점 | 4 | **12** | +8 |
| 경고 | 12 | 12 | 같음 |

근거 인용 매칭 (13건 합계):

| | exact | normalized | ambiguous | unmatched | none |
| --- | --- | --- | --- | --- | --- |
| 기준값 | 206 | 6 | 0 | 6 | 113 |
| gemma4:26b | 205 | 0 | 1 | **38** | 111 |

### 3.3 100건 전체 합계

| 항목 | 값 |
| --- | --- |
| 쟁점 | 347개 (이름뿐 0, 근거 없음 96, 분기 추출 실패 1건 — `CASE000520201703232016075000048`) |
| 사건당 쟁점 수 | 2개 9건 / 3개 44건 / 4개 38건 / 5개 9건 |
| I 노드 | 1,313개 (제목형 8) |
| RA | 792개 (미분류 32, 쟁점에 붙은 RA는 정답 13건 기준 62) |
| 사건당 RA 수 | 5개 9건 / 7개 45건 / 9개 37건 / 11개 9건 — 여전히 쟁점 수의 함수 (09-16 로그 1.7) |
| 역할 채움 | 940/1,208 (여러 역할 0, 별칭 0, 임의 이름 0) |
| 근거 매칭 | exact 1,377 / normalized 31 / ambiguous 4 / **unmatched 248** / none 792 |
| 경고 | 113 |

scheme 분포: `issue_resolution` 346, `evidence_to_hypothesis` 119, `issue_aggregation` 100, `witness_testimony` 67, `best_explanation` 67, `unclassified` 32, `alternatives` 20, `inconsistent_commitment` 14, `effect_to_cause` 10, `sign` 8, `ignorance` 5, `verbal_classification` 4

---

## 4. 해석할 때 주의할 점

1. **순수한 모델 A/B가 아니다.** 기준값(flow `47991b91`) 이후 브랜치에 들어간 변경이 함께 반영됐다: 쟁점 최대 5개(이전 3개), 컨텍스트 32k(이전 16k), RA 줄이기, 쟁점 관계 전용 scheme 등. 미분류 감소나 선택 쟁점 증가는 flow 변경 영향일 수 있다.
2. **정답 비교 표본은 13건이다.** 5,000자 이하만 골라 긴 판결문(09-16 로그 1.4에서 덮음률이 가장 나빴던 구간)은 빠져 있다. 57%를 전체 기준값 40%와 바로 비교하면 안 된다.
3. **"정답 I 덮음"은 원문 구간 겹침 지표다** (09-16 로그 1.1). 내용 일치를 뜻하지 않는다.
4. `unmatched` 증가의 원인은 **확인하지 않았다.** 모델이 인용문을 다듬어 쓰는 것으로 추정하지만 원문 대조는 안 했다.

---

## 5. 현재 상태

- 커밋 안 됨: `langflow/make_v11_flow.py`, `langflow/TopDown_Judgment_to_AIF_v11_Top3Issues.json`, `backend/scripts/eval_runs.py`
- `.env`: `e8ee8cec…` flow를 가리킴 (gitignore 대상)
- 결과: `test_outputs/eval/gemma4-26b-short100/` (`summary.md`, `summary.json`, `runs/`, `run.log`)
- 백엔드(127.0.0.1:8000)는 Claude Code 세션 안에서 실행 중. SSH 터널은 두 개가 떠 있다(11434 사용 중, 11500은 쓰지 않음 — 꺼도 됨)

---

## 6. 할 일

- [x] **`unmatched` 248개 원인 확인** → 8.1
- [x] **원문에서 근거를 못 찾은 쟁점 96개** 확인 → 8.2
- [x] **모델 효과만 분리** → 7
- [ ] 5,000자 초과 정답 8건 실행 — 긴 판결문에서 32k 컨텍스트와 26b가 나아지는지 (최장 20,115자 건 포함)
- [x] 변경 파일 커밋 (`35076b3`에 포함)
- [x] 쓰지 않는 flow `5e7aeb76` 삭제 (사용자). 11500 터널은 PC 재시작으로 이미 없었음
- [ ] 09-16 로그 3.2~3.4 항목은 그대로 유효 (`ra_claim` 제거, 토폴로지 가변화 등)

---

# 09-17 저녁 작업

## 7. 모델 효과 분리 — `gemma4-e4b-gold13`

### 7.1 실행 환경 점검

- PC가 재시작돼 SSH 터널이 없었고, **PC에 설치된 Ollama(0.34.1)가 11434 포트를 차지하고 있었다.** 이 Ollama에는 `gemma4:26b`와 `gemma4:e4b`가 없다. PC Ollama를 끄고 터널을 다시 연 뒤 버전이 0.30.10(서버)으로 나오는지 확인했다.
- Langflow의 26b flow `e8ee8cec`를 보니 **Main Claim LLM의 `model_name`이 빈 값**으로 바뀌어 있었다. 09-16 실행 기록(`pipeline.models`)에는 `gemma4:26b`로 남아 있어 100건 결과는 영향이 없다. Langflow가 PC Ollama 모델 목록으로 값을 새로 고치면서 비운 것으로 추정한다(확인 안 함). 9절에서 flow를 갱신하면서 다시 채웠다.

### 7.2 방법

- 26b flow JSON에서 LLM 노드 5개의 `model_name`만 `gemma4:e4b`로 바꾼 사본을 API로 올렸다. flow ID는 `4023c668-74a5-5985-b0a9-0df9f9af7707`이고, 노드 ID는 그대로 유지된다.
  - 올리기 스크립트는 세션 임시 폴더에 있다(저장소에 넣지 않음).
  - 올린 뒤 Langflow에 있는 26b flow와 비교해 노드, 엣지, 설정이 모델 값 말고는 같은지 확인했다.
- `python scripts/eval_runs.py --label gemma4-e4b-gold13 --set gold21 --max-length 5000 --flow-id 4023c668-74a5-5985-b0a9-0df9f9af7707`
- 실행 중 서버 `/api/ps`로 `gemma4:e4b`(ctx 32,768)만 올라가 있는 것을 확인했다. 실행 기록의 모델도 5개 모두 `gemma4:e4b`였다.

### 7.3 결과

**13건 중 4건 실패.** 모두 쟁점 선택기의 구조 검증에서 재시도를 다 쓴 뒤 `INVALID_SELECTION`으로 멈췄다. 같은 13건을 26b로 돌렸을 때는 실패가 0건이었다.

| 사건 | 실패 사유 |
| --- | --- |
| `CASE000210201808172018075000380` | 두 쟁점에 같은 인용을 근거로 달았음 |
| `CASE000210201811302018075000620` | ISS-013을 두 번 선택 |
| `CASE000210201904172018075001008` | ISS-013을 두 번 선택 |
| `CASE000210201906142019075000106` | ISS-013을 두 번 선택 |

**성공한 9건끼리 비교.** 기준값은 예전 flow `47991b91`에 `gemma4:e4b-it-qat`를 쓴 결과다.

| 지표 | e4b (새 flow) | 26b (새 flow) | 기준값 |
| --- | --- | --- | --- |
| 걸린 시간 | 1,234초 | 1,923초 | 6,208초 |
| 쟁점 적중 / 선택 (정답 27) | 8 / 22 | 12 / 32 | 9 / 27 |
| 정밀도 / 재현율 | 36% / 30% | 38% / 44% | 33% / 33% |
| 정답 I 덮음 (95) | 37 (39%) | 53 (56%) | 47 (49%) |
| I 노드 / 제목형 | 90 / 7 | 125 / 0 | 118 / 0 |
| RA / 미분류 | 53 / 1 | 73 / 3 | 77 / 18 |
| 역할 채움 | 56/88 (64%) | 87/112 (78%) | 68/136 (50%) |
| 근거 exact / normalized / unmatched | 101 / 3 / 8 | 129 / 0 / 28 | 136 / 5 / 4 |
| 근거 없는 쟁점 | 1 | 8 | 3 |
| 주 주장 일치 | 7 | 7 | 8 |

해석:

1. **flow가 같으면 26b가 e4b보다 낫다.** 26b는 구조 검증 실패가 없고 재현율과 정답 I 덮음률이 높다. → **모델은 `gemma4:26b`로 유지하기로 결정.**
2. e4b는 쟁점을 적게 고르고(사건당 2.4개, 26b는 3.6개) 그래프도 작다. e4b의 `unmatched`가 적은 데는 인용 수가 적은 영향도 있다.
3. 미분류 RA가 18에서 1~3으로 준 것은 두 모델 모두에서 나타나므로 **flow 변경의 효과**로 본다.
4. 실패한 4건을 뺀 비교라 e4b 쪽이 좋게 보일 수 있다.
5. **바이트 토큰은 26b만의 문제가 아니다.** e4b도 `…596` 사건의 "핥"을 `<0xED><0x95><0xA5>`로 출력했다(노드 문장 4곳). 기준값에는 없었다. 모델보다는 서버 Ollama(0.30.10)의 디코딩 문제일 가능성이 높다고 추정한다(확인 안 함).

결과: `test_outputs/eval/gemma4-e4b-gold13/`

---

## 8. 결과 파일 분석 (서브 에이전트, 26b 100건 대상)

### 8.1 `unmatched` 인용 248개 원인

**원문을 베껴 쓰다가 글자가 깨진 경우가 가장 많고, 엉뚱한 대목을 인용한 경우는 0개였다.**

| 원인 | 개수 | 예 |
| --- | --- | --- |
| 출력 글자 깨짐 | 128 (52%) | `진술→진찰`, `일관되게→일관되ly`, `폭행한→폭មាន`, `삽입한 사실을 인정→삽验证` |
| 문장 다듬기 | 49 (20%) | 각주 번호 `3)` 삭제 15, 조사·어미 교체 17, 앞머리 어구 추가 10, 원문 오탈자 교정 5 |
| 매칭기가 놓침 | 24 (10%) | 바이트 토큰 `<0xHH>` 미복원 19, 모델이 끼워 넣은 기호(`$⏎`, `/` 등) 5 |
| 인용 잘림·이어붙이기 | 24 (10%) | `...`, `(중략)`으로 원문을 건너뜀 |
| 인용을 비움 | 23 (9%) | 인용이 비어 "쟁점: …" 문장을 대신 매칭 → 구조적으로 항상 unmatched |

- 인용이 길수록 많다: 80자 미만 6%, 400자 이상 46%. 인용 1,000자당 약 0.79회 글자가 따로 깨진다고 보면 관측치와 맞다.
- 깨진 인용 128개 중 120개는 같은 노드의 문장은 멀쩡하다. 원문을 잘못 이해한 게 아니라 출력할 때 생긴 잡음이다.
- 단계별 unmatched 비율: 쟁점 선택 근거 27.7%, 상위 I 15.0%, 하위 I 11.1%, 주 주장 4.0%. 1,000자당 깨짐은 쟁점 선택 단계가 I 노드 단계의 약 2배다.
- `<0xHH>`는 노드 문장에도 9개, 요약에도 1개 남아 화면에 깨진 채로 보인다.
- 줄일 수 있는 양(추정):
  - 매칭 보완 약 190~215개: 바이트 토큰 복원, 기호·각주 번호 무시, 허용 오차 매칭(`fuzzy` 같은 별도 상태)
  - 프롬프트 보강 약 40~70개: 생략 표시 금지, 앞뒤에 말 붙이지 않기, 인용 필수화
  - 한 글자 차이로 뜻이 뒤집히는 경우(피해자↔피고인)가 있어, 허용 오차 매칭은 "정확히 일치"와 구분해야 한다.
- 약한 프롬프트 문구(`make_v11_flow.py`):
  - `EVIDENCE_RULES`에 생략 표시 금지와 각주 번호·오탈자 유지 규칙이 없다.
  - "fully supports"가 긴 인용을 유도한다.
  - 빈 인용을 허용하는 규칙이 선택 기준 "b. a grounding passage exists"와 충돌한다.
  - `BRANCH_PROMPT`의 "Minimal cleanup"이 인용에도 적용되는 것처럼 읽힌다.

결과: `test_outputs/eval/gemma4-26b-short100/analysis/unmatched/` (`analyze_unmatched.py`, `unmatched_quotes.csv/json`, `tables.md`)

### 8.2 근거 없는 쟁점 96개

**"상한을 5로 올려서 약한 쟁점이 끼어든다"는 가설은 "근거 없음" 지표로는 확인되지 않았다.**

| 순번 | 1 | 2 | 3 | 4 | 5 |
| --- | --- | --- | --- | --- | --- |
| 근거 없음 / 선택 | 27/100 (27%) | 31/100 (31%) | 22/91 (24%) | 12/47 (26%) | 4/9 (44%) |

- 1~3번째 27.5%, 4~5번째 28.6%로 거의 같다. 사건당 쟁점 수별로도 26~33%로 평평하다.
- 96개는 8.1의 쟁점 선택 단계 unmatched와 같은 집합이다. 1~3자 글자 오류 64, 빈 인용 22, 크게 줄이거나 이어붙인 인용 10이다. 분기 추출 실패(1건)는 이 판정에 들어가지 않는다.
- 근거 없는 쟁점과 있는 쟁점은 선택 이유 길이, I 노드 수, RA 수, 다른 쟁점과의 유사도가 거의 같다. 빈 인용 22개도 분기 I 노드에는 원문 매칭된 근거가 있다.
- **상한 5의 부작용은 적중률에서 보인다.** 정답 13건에서 4~5번째 쟁점 적중은 1/12(8%), 1~3번째는 13/37(35%)이다.
- 선택 목록을 잘라 본 시뮬레이션(정답 13건). 모델에게 실제로 3개만 고르게 한 결과와는 다를 수 있다.

| 조치 | 선택 | 적중 | 정밀도 | 재현율 |
| --- | --- | --- | --- | --- |
| 현재 (상한 5) | 49 | 14 | 28.6% | 38.9% |
| 상한 3 | 37 | 13 | 35.1% | 36.1% |
| 근거 없는 쟁점 버리기 | 37 | 12 | 32.4% | 33.3% |

- 근거 없는 쟁점을 그냥 버리는 것은 비추천. 대부분 글자 오류라 정답을 맞힌 쟁점 2개도 함께 잃는다.

결과: `test_outputs/eval/gemma4-26b-short100/analysis/no_evidence_issues/` (`analyze.py`, `no_evidence_issues.csv/json`, `issues.csv`, `cases.csv`, `tables.json`)

---

## 9. 결정과 변경 — 쟁점 상한 5 → 3

결정: **모델은 `gemma4:26b` 유지, 쟁점 상한은 3.** 매칭기 보완처럼 그래프 품질을 바꾸지 않는 작업은 뒤로 미루고 실험을 먼저 더 돌린다.

| 파일 | 변경 |
| --- | --- |
| `langflow/components/{issue_selector,issue_branch_extractor,aif_graph_builder,result_validator}.py` | `MAX_SELECTED = 3`, 설명의 "at most 5" → "at most 3" |
| `backend/app/services/catalogs.py` | `MAX_SELECTED_ISSUES = 3` |
| `frontend/src/types/scheme.ts` | `MAX_SELECTED_ISSUES = 3` |
| `langflow/make_v11_flow.py` | 쟁점 선택 프롬프트에서 "Some have 4." 삭제. "If you found more separate grounds than {max_issues}, keep only the {max_issues} most central to the court's conclusion." 추가. flow 설명을 "at most 3"으로 |
| `backend/tests/test_flow_components.py`, `test_api.py`, `test_adapter_v11.py` | 상한 3 기준으로 수정. 상한까지 채운 그래프가 통과하는지는 상수로 계산 |
| `frontend/scripts/v2Smoke.ts` | 샘플 쟁점이 3개라 "상한 안 수락 허용" 검사를 쟁점 하나를 뺀 그래프로 수행 |
| `langflow/TopDown_Judgment_to_AIF_v11_Top3Issues.json`, `components/_built_nodes.json` | Langflow Desktop Python으로 재생성 (11 노드 / 20 엣지, LLM 5개 `gemma4:26b`) |

- 프롬프트는 상한과 모순되지 않도록 한 줄만 고쳤다. 그 밖의 쟁점 선택 규칙("상한은 목표가 아니다", "근거 하나에 쟁점 하나" 등)은 그대로다.
- 검증:
  - 백엔드 테스트 102개 통과
  - 프런트 `tsc -b` 통과
  - `smoke`, `smoke:annotation`, `smoke:store`, `smoke:v2` 모두 통과
- Langflow 반영: 기존 flow `e8ee8cec`를 `PATCH /api/v1/flows/{id}`로 제자리 갱신했다. `.env`는 바꾸지 않았다.
  - 갱신 후 선택기, 추출기, 그래프 빌더, 검증기 모두 `MAX_SELECTED = 3`이다.
  - Main Claim을 포함한 LLM 5개 모두 `gemma4:26b`다.
- 백엔드는 상한 상수를 다시 읽도록 재시작했다. `/api/pipelines`에서 `mode=live`, 분석 flow `e8ee8cec…`를 확인했다.

## 10. 현재 상태 (09-17 저녁)

- 커밋 안 됨: 9절 표의 파일들, 이 로그
- Langflow:
  - `e8ee8cec` = 26b + 상한 3 (분석 flow)
  - `4023c668` = e4b 사본 (상한 5, 더 쓰지 않음, 지워도 됨)
- **Langflow UI에서 flow를 열면 Main Claim 모델 값이 다시 비워질 수 있다.** 실행 전에 `/api/pipelines` 또는 실행 기록의 `pipeline.models`로 확인할 것.
- 백엔드(127.0.0.1:8000)는 Claude Code 세션 안에서 실행 중이다. SSH 터널은 11434, PC Ollama는 꺼 둔 상태다.

## 11. 할 일

- [ ] **상한 3 재평가** — 26b 100건 세트와 같은 조건으로 재실행해 5절 결과와 비교
  - 예: `python scripts/eval_runs.py --label gemma4-26b-cap3-short100 --set all --max-length 5000 --limit 100`
  - 정답 13건만 먼저 보려면 `--set gold21 --max-length 5000`
- [ ] 5,000자 초과 정답 8건 실행 (6절에서 이어짐)
- [ ] 그래프 품질에 영향이 있는 인용 개선. 실험 결과를 보고 결정한다.
  - 인용 프롬프트 보강: 생략 표시 금지, 앞뒤에 말 붙이지 않기, 각주 번호·오탈자 유지
  - 선택기에서 빈 인용 거절
  - 노드 문장과 요약의 `<0xHH>` 복원
- [ ] (보류) 매칭기 보완: 바이트 토큰 복원, 기호·각주 번호 무시, `fuzzy` 상태
- [ ] (보류) 바이트 토큰 원인 확인 — 서버 Ollama 버전과 디코딩 설정
- [ ] e4b 사본 flow `4023c668` 삭제
- [ ] 변경 파일 커밋
